#include "PositionComparator.h"
/*
State::State(const FPosition& PositionArg, ATeamLeader* Team)
	: Position(PositionArg), Team(Team)
{

}

FPosition PositionComparator::GetBetterPosition(const FPosition& Origin, int32 MaxDepth, AGrid* Grid, ATeamLeader* Team)
{
	TArray<State> Fringe = {Origin};

	Fringe.HeapPush(State(Origin), StatePredicate(Team))
}
*/